package com.example.smarthome.data.model

/**
 * Represents the current operational connectivity or health status of a device.
 */
enum class DeviceStatus {
    ON,
    OFF,
    ERROR,
    DISCONNECTED
}
